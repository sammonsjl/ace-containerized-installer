# ace.containerized_installer

Roles that deploy the ACE control plane (gateway + AWX controller) as rootless
podman containers managed by systemd user units — the same mechanics the AAP
containerized installer uses, rebuilt from scratch on upstream images.

See the repository root README for usage.

# {{ ansible_managed }}
# Unix-socket-only instance for the controller (broker db 0, cache db 1).
port 0
bind 127.0.0.1
unixsocket /run/redis/redis.sock
unixsocketperm 777
dir /data
save 60 1
loglevel warning
